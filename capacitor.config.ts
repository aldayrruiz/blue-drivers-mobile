import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.bluece.drivers',
  appName: 'BLUE Drivers',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
